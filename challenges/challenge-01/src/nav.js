import Button from './button'

function Nav () {
  return (
    <nav>
      <Button kind='primary'>Home</Button>
      <Button kind='secondary'>See more...</Button>
    </nav>
  )
}

export default Nav
